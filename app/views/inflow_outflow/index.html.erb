<!-- app/views/inflow_outflow/index.html.erb -->
<div class="min-h-screen bg-slate-950 px-4 py-8 text-slate-100 sm:px-6 lg:px-8">
  <div class="mx-auto w-full max-w-7xl">

    <header class="mb-6">
      <div class="text-xs font-semibold uppercase tracking-[0.25em] text-cyan-300">
        Exchange Flow Intelligence
      </div>

      <h1 class="mt-3 text-4xl font-bold tracking-tight text-white">
        Inflow / Outflow
      </h1>

      <p class="mt-3 max-w-3xl text-sm leading-relaxed text-slate-400">
        Analyse des flux Bitcoin vers et depuis les adresses exchange-like.
      </p>
    </header>

    <section class="rounded-2xl border border-gray-800 bg-gray-900/20 overflow-hidden">
      <%= render "dashboard/true_exchange_flow" %>
    </section>

  </div>
</div>