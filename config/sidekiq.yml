:concurrency: 1

# Les queues strictes et Heavy Tansa sont volontairement consommées par
# des processus dédiés dans Procfile.dev. Ce fichier reste conservateur
# pour éviter qu'un lancement `bundle exec sidekiq` nu crée un worker
# générique concurrent.
:queues:
  - [scheduler, 1]
